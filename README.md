  # Baby_Machine_Learning
  
## N-gram Language Model (CPU)
Run:
```powershell
$uv = "$env:USERPROFILE\.local\bin\uv.exe"
& $uv run python .\ngram_lm.py

## 3) Commit + push so it’s ready to show
```powershell
git add ngram_lm.py README.md
git commit -m "Add simplest CPU n-gram language model demo"
git push
